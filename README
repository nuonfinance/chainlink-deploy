## Setting up variables
Set up `.env` variables and variables in the configuration files:

1. `chainlink/config.toml`
- `<chain_id>`
- `<chain_name>`
- `<link_contract_address>`
- `<wss_endpoint>`
- `<http_endpoint>`

2. `chainlink/secrets.toml`
- `<keystore_password>`
- `<db_password>`

3. `.api`
- `<admin_password>`
